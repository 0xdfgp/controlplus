node_modules/
.env
.expo/
dist/
.omc/
!.omc/skills/

# Generated by `expo prebuild`. The app.json config is the source of truth,
# so the native projects are rebuilt rather than committed.
apps/mobile/ios/
apps/mobile/android/

# The same output, from `expo prebuild` run at the repo root by mistake. The
# mobile app is a workspace, so nothing native belongs here. Anchored to the
# root so a legitimate apps/**/ios/ is still governed by the rules above.
/ios/
/android/
/app.json

# TypeScript incremental build state, regenerated by `tsc -b`.
*.tsbuildinfo

*.log
.DS_Store
